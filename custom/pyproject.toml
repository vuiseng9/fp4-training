[project]
name = "xops"
dynamic = ["version"]
dependencies = ["torch>=2.0.0", "pytest>=7.0.0", "torchao==0.12.0"]

[build-system]
requires = ["setuptools>=64", "wheel", "torch", "ninja"]
build-backend = "setuptools.build_meta"