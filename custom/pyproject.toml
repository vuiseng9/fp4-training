[project]
name = "xops"
dynamic = ["version"]
dependencies = ["torch>=2.0.0", "pytest>=7.0.0"]

[tool.setuptools]
packages = ["backend", "linear"]

[build-system]
requires = ["setuptools>=64", "wheel", "torch", "ninja"]
build-backend = "setuptools.build_meta"